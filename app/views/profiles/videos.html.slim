= render layout: 'page' do
  h2.subheadline = @page_title.last
  div
    - if @collection.any?
      = render partial: 'moderation/anime_video_reports/report', collection: @collection
      - if @add_postloader
        = render 'blocks/postloader', url: videos_profile_url(page: @page+1)
    - else
      p.b-nothing_here Нет загрузок и правок видео
