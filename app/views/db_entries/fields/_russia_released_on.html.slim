= render 'application/note_from_club_page', club_page_id: 2905, is_version: true

.cc-2
  .c-column
    = simple_form_for resource.object do |f|
      = f.input :russia_released_on,
        start_year: Time.zone.now.year + 3,
        end_year: 1920,
        include_blank: true

      = f.input :russia_released_on_hint

      = render 'versions/reason'

      span = "#{t('previous_value')}: #{resource.object.russia_released_on}"

      .buttons
        a.cancel href=resource.edit_url = t 'actions.cancel'
        = f.button :magic_submit
