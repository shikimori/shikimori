- if poster
  picture
    source[
      src=poster.image(:small).url
      srcset='#{poster.image(:medium).url} 2x'
      type='image/webp'
    ]
      / src=cdn_image_url(anime, :preview)
      / srcset="#{cdn_image_url anime, :original} 2x"
      / alt=(local_assigns[:db_entry] || poster.target).name
    img[
      alt=db_entry.name
      src=poster.image(:small_alt).url
      srcset='#{poster.image(:medium_alt).url} 2x'
      loading='lazy'
    ]
- else
  img[
    alt=db_entry.name
    src='/assets/globals/missing_original.jpg'
    loading='lazy'
  ]
