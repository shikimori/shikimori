.screenshots-uploader
  span.item-content-editor-buttons
    span.edit-controls
      span.control.item-upload
        | Загрузить
        span
          input[type="file" multiple]
  .subheadline Загрузка новых скриншотов
  .subcontent.m0
    .comment-block
      .description.upload-area data-upload_url="#{screenshots_url @entry}"
        p.m5 Перед загрузкой кадров, пожалуйста, удостоверьтесь, что загружаемое удовлетворяет следующим условиям:
        ul.m10
          li
            p Кадры должны быть надлежащего качества: онгоинги - 720p (1280x720), для более старых - не менее 480p (720x480), для совсем старых аниме 90х годов и ранее можно и в хушдем качестве;
          li
            p На кадрах не должно быть логотипов, водяных знаков других сайтов, а так же субтитров;
          li
            p Мало кадров - это не интересно, загружайте, пожалуйста, по 30-50 штук;

        .inner.drag-here-placeholder
          p Перетаскивайте сюда картинки, или тыкайте на кнопку &laquo;Загрузить&raquo; справа вверху. Как перетаскиванием, так и кнопкой "Загрузить", загружать можно до ста картинок за раз (выделяйте сразу несколько файлов);
          = render partial: 'blocks/upload_progress', formats: :html
          .screenshots
            .images-list.uploaded-container.subcontent
            .clearfix
          p.thank-you Спасибо. Загруженное будет проверено модератором, после чего вам придёт уведомление.
        .clearfix

- if @entry.screenshots.any? || @entry.changes[:screenshots].any?
  .screenshots-editor.item-editor-container data-kind="#{@entry.object.class.name.downcase}"
    - if @entry.screenshots.any?
      = render partial: 'site/editor_buttons', locals: { apply_path: self.send("apply_#{@entry.object.class.name.downcase}_url", @entry), item_name: @entry.object.class.name.downcase }, formats: :html
      .subheadline.m10 Изменение кадров аниме

      .screenshots-title.subcontent.m5
        p По наведению на кадр появится крестик - нажав на него, можно удалить картинку.
        p Порядок кадров меняется перетаскиванием картинки и нажатием на кнопку сохранить, расположенную выше.

      = render partial: 'site/editor_textfield_new', locals: { item: @entry, field: 'screenshots', action: UserChange::ScreenshotsPosition }, formats: :html

      .screenshots
        .images-list.screenshots-position.subcontent
          = render partial: 'images/image', collection: @entry.screenshots, locals: { group_name: 'screenshots', style: :original, alt: "Кадр из #{@entry.name}", with_deletion: true }, formats: :html

    - if @entry.changes[:screenshots].any?
      .no-change-details
        = render partial: 'site/editor_changes', locals: { changes: @entry.changes[:screenshots], moderation: true }, formats: :html
