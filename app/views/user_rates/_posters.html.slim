.list-posters.cc-5.to-process data-dynamic='cutted_covers'
  - list.entries.each do |user_rate|
    .c-column.user_rate[
      data-target_id="#{user_rate.target_id}"
      data-title="#{user_rate.target_name}"
      data-text="#{user_rate.text}"
    ]
      = render user_rate.target.decorate,
        cover_notice: :year_kind,
        content_by: :mini_tooltip,
        tooltip_predelay: 1000
      - if library.user.own_profile?
        .edit-user_rate[
          data-remote="true"
          data-action="#{edit_api_user_rate_path user_rate, detailed: true}"
          data-type='html'
          data-rate_url="#{api_user_rate_path user_rate}"
        ]
