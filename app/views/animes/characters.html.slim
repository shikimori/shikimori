= render layout: 'animes/page' do |f|
  - if @entry.roles.main_characters.any?
    .block.m0
      h2.subheadline.m10 = t 'Role.Main'
      .cc.m0
        = render partial: 'characters/character', collection: @entry.roles.main_characters, locals: { content_by: :tooltip }, formats: :html

  - if @entry.roles.supporting_characters.any?
    .block.m0
      .b-options-floated.collapse
        span.action свернуть (#{@entry.roles.supporting_characters.size})
      h2.subheadline.m10 = t 'Role.Main'
      .collapsed.tiny ...
      .cc.m0
        = render partial: 'characters/character', collection: @entry.roles.supporting_characters, locals: { content_by: :tooltip }, formats: :html

  - if @entry.roles.people.any?
    .block.m0
      .b-options-floated.collapse
        span.action свернуть (#{@entry.roles.people.size})
      h2.subheadline.m10 Создатели #{@entry.anime? ? 'аниме' : 'манги'}
      .collapsed.tiny ...
      .cc.m0
        = render partial: 'people/person', collection: @entry.roles.people, formats: :html
