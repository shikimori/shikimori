.b-list_switchers
  - values.each do |value|
    span.switcher[
      class="#{value} #{:selected if cookies[name] == value || (!cookies[name] && value == values.first)}"
      data-name="#{name}"
      data-value="#{value}"
    ]
