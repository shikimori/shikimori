//$('.entry-content-slider .comments form').live('comment:added', function() {
  //increment_comments_num($('.slider-control-comments a .num, .slider-control-comments span .num'));
//});
//$('.entry-content-slider .comments form').live('comment:deleted', function() {
  //decrement_comments_num($('.slider-control-comments a .num, .slider-control-comments span .num'));
//});
// переход на вкладку комментариев
//$('.comments-nav').live('click', function() {
  //if ($(window).scrollTop() > 200) {
    //$.scrollTo($('h1').offset().top - 10);
  //}
  //$('.slider-control-comments').trigger('click');
//});
