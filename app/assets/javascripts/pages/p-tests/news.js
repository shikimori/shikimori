pageLoad('tests_news', () => {
  // process($('.b-news_wall'));
  //
  // $('.b-news_wall').on('postloader:before', (_e, $data) => {
  //   process($data);
  // });
});

function process($nodes) {
  // $nodes.find('.b-news_wall-topic').each((_index, node) => {
  //   console.log(node);
  // });
}
